package com.exl.rdaas.util;

import java.io.IOException;
import java.io.UncheckedIOException;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;

public class Parser {
	
	/*
	 * // private static final XmlMapper XML_MAPPER = new XmlMapper(); private
	 * static final ObjectMapper OBJECT_MAPPER = new ObjectMapper(); public static
	 * String serialize(String requestBody, String format) { return
	 * format.equals("application/json") ? requestBody :
	 * format.equals("application/xml") ? serialize (requestBody, XML_MAPPER) : "";
	 * }
	 * 
	 * private static String serialize(String requestBody, XmlMapper xmlMapper) {
	 * try { JsonNode node = xmlMapper.readTree(requestBody.getBytes()); return
	 * OBJECT_MAPPER.writeValueAsString(node); } catch (IOException e) { throw new
	 * UncheckedIOException(e); } }
	 */
	}
